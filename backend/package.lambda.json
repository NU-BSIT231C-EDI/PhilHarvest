{
  "name": "philharvest-lambda-worker",
  "version": "1.0.0",
  "description": "AWS Lambda handler for processing EDI queue jobs from SQS",
  "main": "lambda.js",
  "scripts": {
    "package": "zip -r lambda.zip lambda.js node_modules",
    "test": "node lambda.js"
  },
  "keywords": [
    "aws",
    "lambda",
    "sqs",
    "edi",
    "queue"
  ],
  "author": "PhilHarvest",
  "license": "UNLICENSED",
  "dependencies": {
    "axios": "^1.6.0"
  }
}
